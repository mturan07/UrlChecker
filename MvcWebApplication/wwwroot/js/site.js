/**
 * 
 *  Some JS commands
 * 
 *  The quick and simple editor for cron https://crontab.guru
 *  
 */

// $(document).ready(function () { 
//    $('#selectCron').change(function () {
//        var cron = this.value;       
//        var text = "";
        
//        if (cron == "m") { text = "* * * * *" } else
//            if (cron == "m5") { text = "*/5 * * * *" } else
//                if (cron == "h") { text = "" } else
//                    if (cron == "d") { text = "" } else
//                        if (cron == "w") { text = "" } else
//                            if (cron == "c") { text = "" }
//        $("#textCron").val(cron);
//    });
//});